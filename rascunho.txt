 render[i] = (
          <div className="switch" key={i}>
            <ul>
              <li> <lable>  {Helpers.socialIcon(services[i]['socialName'], 2)}   </lable>
                <label>
                  Off
              <input type="checkbox" checked={this.state.checkedService} onChange={() => this.handleChange(services[i]['socialName'])} />
                  <span className="lever"></span>
                  On
              </label></li>
            </ul>
          </div>);
      }