https://graph.facebook.com/v2.5/me/feed?fields=id,from,story,message,message_tags,place,shares,source,to,link,comments,attachments{media},created_time,description,likes,sharedposts&limit=25

/*
- This function receives the information by server and pass to the presentation component
*/

export default DrawerContainer = createContainer(() => {


  let dataProfile = {};
  let self = this;

    Meteor.call('getFacebookProfile', function(error, result){
     if(error){
      console.log(error);
    }
    console.log(result.user);
    dataProfile = result.user;
    console.log(dataProfile);
    return result.user; 
  });



  // Do all your reactive data access in this method.
  // Note that this subscription will get cleaned up when your component is unmounted
  var handle = Meteor.subscribe("users");
 
    console.log(dataProfile);
  return {
    loading: !handle.ready(),
    currentUser: Meteor.user(),
  //   this.data = result; 
  //   console.log(result);
  //   console.log(result.user);
  //  // dataProfile = result.user;
  //   console.log(dataProfile);
  //   return result.user; 
  };
}, Drawer);